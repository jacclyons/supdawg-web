import { ComingSoon } from "@/components/layout/ComingSoon";

export const metadata = { title: "Custom Orders — SUPDAWG!" };

export default function Page() {
  return (
    <ComingSoon
      title="Custom Orders"
      description="The custom order form is on the next stitch. Hold tight — we're building it now."
    />
  );
}
